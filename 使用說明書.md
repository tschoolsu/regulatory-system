# 法規系統使用說明書

臺北市數位實驗高級中等學校第五屆學生會 法規系統 — 本文件說明如何維護本系統（新增法規、本機測試與發布到 GitHub Pages）。本文件提供給負責維護的管理員使用。

---

## 一、系統簡介

本系統為**純前端**網站，不需要資料庫或後端伺服器。法規內容以 Markdown 檔案儲存在 `regulations/` 資料夾中，建置時自動匯入、依資料夾結構分類，發布後即可在網頁上查閱。

技術：Vite + React + TypeScript，Markdown 渲染採用 unified / remark / rehype 管線（支援 GFM、KaTeX、Mermaid、HackMD 語法）。

## 二、目錄結構

```
tpass-regulatory/
├─ regulations/            ← 法規內容（主要維護區域）
│  ├─ 校規/
│  │  ├─ 個人行為/
│  │  │  └─ 1.md
│  │  └─ 集體行為/
│  │     └─ 社團活動管理要點.md
│  ├─ 學生會/
│  │  └─ 組織章程.md
│  └─ 附錄/
│     └─ 法規修訂流程.md
├─ src/                    ← 前端程式碼（一般不需更動）
├─ .github/workflows/      ← GitHub Actions 自動部署設定
└─ index.html / vite.config.ts
```

## 三、新增 / 修訂法規

### 3.1 資料夾結構就是分類

法規檔案放進 `regulations/` 下的資料夾，資料夾路徑即分類路徑：

- `regulations/校規/個人行為/1.md` → 分類：**校規 > 個人行為**
- `regulations/學生會/組織章程.md` → 分類：**學生會**（根分類）

可任意增加階層，沒有深度限制。檔案名稱建議使用有意義的名字，但**標題一律寫在 frontmatter**，網站顯示的是 `title`。

### 3.2 檔案開頭（frontmatter）

每個檔案最上方用 YAML 定義 metadata，以 `---` 包夾：

```markdown
---
title: 法規名稱
revision: 2026-08-25
tags: [校規, 個人行為]
author: 學生會權益部
---

法規內文……
```

| 欄位      | 必填 | 說明                                                        |
| --------- | ---- | ----------------------------------------------------------- |
| `title`   | 否   | 顯示標題；省略時以檔名代替                                  |
| `revision`| 否   | 最後修訂日期（`YYYY-MM-DD`），用於「最新修訂」排序           |
| `tags`    | 否   | 標籤清單，用於搜尋與分類頁顯示                              |
| `author`  | 否   | 提出單位，顯示在法規頁頂部                                  |

省略 `revision` 的法規在「最新修訂」清單不會出現，建議每份法規都填寫。

### 3.3 支援的 Markdown 語法

內文可自由使用下列語法（與 HackMD 相容）：

- **YAML metadata**：如上所述
- **目錄**：`[TOC]`（單獨一行，自動依標題生成）
- **色塊提示**：
  ```
  :::info 標題（可省略）
  內容
  :::
  ```
  支援種類：`info`、`note`、`tip`、`success`、`warning`、`danger`、`question`、`quote`、`spoiler`（spoiler 會收合成可展開區塊）
- **GitHub Alerts**：`> [!NOTE]`、`> [!TIP]`、`> [!IMPORTANT]`、`> [!WARNING]`、`> [!CAUTION]`
- **GFM**：表格、刪除線 `~~文字~~`、任務清單 `- [ ]`、自動連結
- **數學公式**（KaTeX）：行內 `$x^2$`、獨立 `$$...$$`
- **圖表**（Mermaid）：` ```mermaid ` 程式碼區塊內寫 flowchart / sequenceDiagram / gantt 等
- **Emoji 短代碼**：`:smile:`、`:tada:` 等
- **腳註**：`文字[^1]`，文末 `[^1]: 註解`
- **單一換行即斷行**：與 HackMD 行為一致

### 3.4 存檔即上架

修改完成後 **push 到 GitHub 的 `main` 分支**，GitHub Actions 會自動重新建置並發布，幾分鐘內網站即更新。

## 四、本機開發（可選）

若要在自己電腦預覽修改結果：

```bash
pnpm install  # 第一次執行（本專案套件管理一律用 pnpm，不要用 npm/yarn）
pnpm dev      # 啟動開發伺服器（http://localhost:5173）
pnpm build    # 產出正式檔案到 dist/
pnpm preview  # 預覽建置結果
pnpm lint     # 程式碼檢查
```

## 五、發布到 GitHub Pages

專案已附自動部署設定（`.github/workflows/deploy.yml`），首次使用只需：

1. 在 GitHub 倉庫的 **Settings → Pages**，於 **Build and deployment** 選擇 **Source: GitHub Actions**
2. 之後每次 push 到 `main` 就會自動部署
3. 正式網站網址為 `https://law.tschoolsu.org`（掛了 T-Pass 生態系的自訂網域）

因為採用了相對路徑與 hash 路由，就算之後拿掉自訂網域、退回
`https://<帳號>.github.io/<倉庫名>/` 這種子路徑，也能正常運作，無需額外設定。

## 六、常見問題

**Q：為什麼新增檔案後網站沒有出現？**
請確認：檔案副檔名是 `.md`、位於 `regulations/` 之下、且已 push 到 `main` 分支並等待部署完成（Actions 約需 1–2 分鐘）。可用本機 `pnpm dev` 先行確認。

**Q：分類名稱可以改嗎？**
可以，直接把資料夾改名並更新檔案位置即可，分類與連結會自動更新。建議一併更新 README。

**Q：可以放圖片或附件嗎？**
可以。目前渲染支援 Markdown 內文中的圖片；若要放置大檔案建議另外上傳並使用外部網址。
